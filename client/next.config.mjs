/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'randomuser.me', 'images.unsplash.com'], // Add your image hostnames here
  },
};

export default nextConfig;
