/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'randomuser.me'], // Add your image hostnames here
  },
};

export default nextConfig;
